{%- comment -%}
  Custom code before </body>
  All custom code in this file will be kept when updating the theme
{%- endcomment -%}

<script>
  window.WowSaleBadgeConfig = {
    locale: {{ request.locale.iso_code | json }},
    currencyCode: {{ localization.country.currency.iso_code | default: cart.currency.iso_code | default: shop.currency | json }},
    currencySymbol: {{ localization.country.currency.symbol | default: cart.currency.symbol | json }}
  };
</script>
<script src="{{ 'wow-sale-badge-money.js' | asset_url }}" defer="defer"></script>
